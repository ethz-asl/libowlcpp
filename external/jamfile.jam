# @file "owl_cpp/external/jamfile.jam"
# part of owl_cpp project.
# Distributed under GNU Lesser General Public License; see doc/license.txt.
# @date 2011 @author Mikhail K Levin

project external
    : build-dir $(OUT)/bin
    : 
;

if ! $(FACTPP) { EXIT "please set FACTPP to location and  version of FaCT++ library; see doc/user-config.jam" ; }
using ext/factpp : $(FACTPP[2]) : $(FACTPP[1]) ;

alias factpp_kernel
    : #sources
        /ext/factpp//factpp_kernel
    : #requirements
;

if ! $(LIBXML) { EXIT "please set LIBXML to location and  version of libxml library; see doc/user-config.jam" ; }
using ext/libxml : $(LIBXML[2]) : $(LIBXML[1]) ;

alias libxml2
    : #sources
        /ext/libxml
    : #requirements
;

